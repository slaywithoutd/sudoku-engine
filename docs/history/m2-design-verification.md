# Expanded M2 design verification

_Historical record, kept as written on 2026-09-12. Branch and worktree names it mentions no longer exist; everything is on `master`._

Date: 2026-09-12. Scope: documentation and planning only, under D046/D059. The solver is not implemented; application test counts in other records are historical application evidence.

## Inspected baseline and preserved work

- Clean `docs/m2-solver-design` at `80471d2`; recent history includes expanded research `24e0d25`, cell inset `9c284de`, English/input `c65e9db` and sidebar `09887f4`.
- No applicable AGENTS.md found in workspace or ancestor directories. Read the required documentation, current model/editor/classic/library/controllers/routes/views, Vite/TypeScript/Playwright configuration and M1/UI/appearance verification.
- Persistent Light/Dark × five pastel themes under D058 remain intact. No executable, dependency, lockfile, generated, Spring/Maven or existing test file was changed. No push, merge or deployment.

## Deliverables and authority

- [Screen/evidence specification](superpowers/specs/2026-09-12-m2-classic-solver-design.md) replaces the obsolete baseline and uses English throughout.
- [Research rationale](superpowers/specs/2026-09-12-m2-engine-expansion-design.md) retains source attribution and points to authoritative concrete contracts.
- [Engine contracts](superpowers/specs/2026-09-12-m2-engine-contracts.md) cover normalization/assembly, shared state/facts/events, discovery, proof checking, scheduling, exact evidence, protocol, limits and persistence.
- [Technique matrix](superpowers/specs/2026-09-12-m2-technique-coverage.md) defines C01–C33/U01–U05 with aliases, prerequisites, assumptions, finite bounds, proof grammar and independent fixture gates. Fixture IDs are future acceptance requirements, not fabricated existing evidence.
- [Revised 27-task plan](superpowers/plans/2026-09-12-m2-classic-solver.md) contains exact file ownership, signatures, dependency sequence, failing assertions, implementation algorithms, verification commands, focused commits and requirement map. All execution checkboxes remain unchecked.
- README, roadmap, platform architecture and D059–D064 distinguish confirmed requirements, concrete recommendations and empirical benchmark questions. Design approval remains required before any solver implementation.

## Contract review outcomes

| Review concern | Resolution |
| --- | --- |
| Old six-technique scope and Portuguese copy | Replaced baseline spec/plan; broad matrix and English D053 are authoritative. Preserve D058 themes. |
| Small all-different scope misused as full house | Separate cover/existence capability with checked premise; synthetic cage counterexample. |
| Unproved candidate initialization | Initialize only clue/rule roots and full unresolved domains; rule exclusions pass normal proof/atomic acceptance and appear in preamble. |
| Mutable source and stale mixed-rule caches | Canonical full semantic identity, isolated branch/revision state, central invalidation, cold rebuild tests. |
| Generalized chains or specialized names treated as proof | Separate named grammar and finite certificates; independent mutation and original-clue fixtures; explicit unsupported generalizations. |
| Assumption/uniqueness leakage into Perfect | Scope-aware discharge plus permanent conditional provenance; independent original-problem uniqueness and complete unconditional path both required. |
| Full trace copies or multi-chunk deadlock | Chunk ACK grants staging credit, separate accepted-step response authorizes next revision; bounded staging and one pending step. |
| Missing ledger framing | At most one live cursor per descriptor/rule; <=256 jobs, bounded ledger-chunk packets and complete logical-stop barrier. |
| Broad search starves count check | Proposed 70% human time/work ceiling; explicit stop-human/human-stopped accepted-revision reconciliation closes pending-proof races, preserving remaining exact budget. Expiry is incomplete logic; benchmark alternatives. |
| Unmeasured runtime defaults | Explicit configurable caps, separate support-versus-speed gates, production/held-out benchmarks, rollout initially off. |
| Lost newer persistence behavior | Memory-only solver service; Save Clues only explicit library write; unchanged D058 settings and backup schema. |

## Verification procedure

Run whitespace and changed-path checks against the inspected baseline; enumerate all tracked/untracked changed files and require Markdown under `docs/`. Compare `web/`, legacy `src/`, Maven files and lockfiles with `80471d2`. Parse local Markdown links outside fenced code blocks and verify both target files and heading fragments. Audit the 27 sequential task headings, 38 unique matrix row IDs, all task coverage references, unchecked execution steps, code fences, decision continuity and absence of unfinished placeholder markers. Finally inspect focused staged diffs before each local commit.

The requirement map at the end of the plan covers every requested design work item. Type ownership/signatures were cross-read between the concrete contracts and producer/consumer tasks, including initial root/propagation ordering, independent exact input, conditional parent identity, byte/node accounting, stream ACK versus accepted-step semantics, deadline ties and result persistence. These are document consistency checks, not a compiled API or a proof of runtime correctness.

Primary-source gap review reused HoDoKu/CSP-Rules research and inspected the authors' fish, ALS, Fireworks, SK Loops, Exocet/Double Exocet and Tridagon descriptions. Specialized uncertainty led to explicit bounded certificate requirements, not inferred complete support. Berthier's chain/braid paper is linked for the distinct grammar basis; its arXiv HTML rendering was unavailable, so no claim of a newly audited full paper is made. No third-party solver or puzzle collection was installed/copied. Source timings do not validate this implementation's defaults.

## Evidence and limitations

Executed documentation checks: 24 Markdown documents, 101 local links including heading fragments, zero broken targets/fragments; 27 sequential tasks with 136 unchecked steps; 38 unique technique matrix rows; balanced M2 code fences and no unfinished placeholder markers. All ten changed/new files are Markdown under `docs/`; application and legacy paths are unchanged from `80471d2`. Whitespace/staged checks and final commit identifiers are recorded with the local commits and handoff. No application tests/builds or solver benchmarks were run during planning because no application code changed. Future plan snippets must be implemented, compiled and tested after approval. All technique discovery/fixture labels, mathematical checker implementations, worker latency, memory ceilings and proposed defaults remain unverified runtime work; the completed output is a reviewable specification and implementation plan.

No unresolved product question blocks design review. Phase share, score calibration, default limits and rollout benefit remain explicit empirical gates. No solver implementation is authorized by this record.
