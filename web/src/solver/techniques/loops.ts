import { chainDescriptor } from "./chains-runtime";
/** Fairly serviced scalar loops, intersection groups and complete ALS visits. */
export const loopTechniques = Object.freeze([chainDescriptor("C17")]);
