import { chainDescriptor } from "./chains-runtime";
/** Scalar alternating paths, including the full 24-link structural search bound. */
export const chainTechniques = Object.freeze([chainDescriptor("C16")]);
