import { alsDescriptor } from "./als-runtime";

/** C19 strategy: <=6-set ALS chains and complete 2–4-way Death Blossom. */
export const deathBlossomTechniques = Object.freeze([alsDescriptor("C19")]);
